import{_ as e,c as i,a0 as a,o as t}from"./chunks/framework.BGo0cstt.js";const u=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"ssh-server-access.md","filePath":"ssh-server-access.md"}'),n={name:"ssh-server-access.md"};function o(l,s,h,p,r,d){return t(),i("div",null,s[0]||(s[0]=[a(`<p>To SSH from your Mac to your Linux machine, you’ll need to ensure both machines are set up properly for SSH communication. Here’s a step-by-step guide for setting everything up from scratch:</p><h2 id="on-linux-mini-pc" tabindex="-1">On Linux (Mini PC): <a class="header-anchor" href="#on-linux-mini-pc" aria-label="Permalink to &quot;On Linux (Mini PC):&quot;">​</a></h2><ol><li><p><strong>Install SSH Server</strong>: If SSH is not already installed on your Linux machine, you’ll need to install the OpenSSH server. To do this:</p><ol><li>Open a terminal.</li><li>Run the following command:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> apt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> update</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> apt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> openssh-server</span></span></code></pre></div></li><li>Once installed, check if the SSH service is running:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> systemctl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> status</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ssh</span></span></code></pre></div></li><li>If SSH is not running, start the service:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> systemctl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> start</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ssh</span></span></code></pre></div></li><li>Ensure the service starts on boot:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> systemctl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> enable</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ssh</span></span></code></pre></div></li></ol></li><li><p><strong>Check Your Linux Machine’s IP Address</strong>: You’ll need the IP address of the Linux machine to connect from your Mac. To find it:</p><ol><li>Open a terminal.</li><li>Run the following command:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> a</span></span></code></pre></div></li><li>Look for the section related to your active network interface (e.g., <code>eth0</code> for wired or <code>wlan0</code> for Wi-Fi) and find the IP address (something like <code>192.168.x.x</code>).</li></ol></li></ol><h2 id="on-mac" tabindex="-1">On Mac: <a class="header-anchor" href="#on-mac" aria-label="Permalink to &quot;On Mac:&quot;">​</a></h2><ol><li><p><strong>Install OpenSSH (if not already installed)</strong>: Most modern macOS systems come with OpenSSH pre-installed, but you can check if it&#39;s installed by running:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -V</span></span></code></pre></div><p>If it shows a version, you’re good to go. If not, you may need to install it using Homebrew:</p><ol><li>Install Homebrew (if you don’t have it):<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">/bin/bash</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -fsSL</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)&quot;</span></span></code></pre></div></li><li>Install OpenSSH:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">brew</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> openssh</span></span></code></pre></div></li></ol></li><li><p><strong>Connect to the Linux Machine</strong>:</p><ol><li>Open a terminal on your Mac.</li><li>Use the SSH command to connect to your Linux machine (replace <code>user</code> with your Linux username and <code>192.168.x.x</code> with the Linux machine’s IP):<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> user@192.168.x.x</span></span></code></pre></div></li><li>The first time you connect, you may be prompted to accept the server’s fingerprint. Type <code>yes</code> and hit Enter.</li><li>Enter your Linux user password when prompted.</li></ol></li><li><p><strong>Configure SSH Key-Based Authentication (Optional but recommended)</strong>: To avoid entering the password each time, you can set up SSH keys:</p><ol><li>On your Mac, generate an SSH key (if you don’t have one already):<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh-keygen</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rsa</span></span></code></pre></div>Press Enter to save the key in the default location (<code>~/.ssh/id_rsa</code>).</li><li>Copy the SSH public key to your Linux machine:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh-copy-id</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> user@192.168.x.x</span></span></code></pre></div></li><li>Now, when you SSH, you should be able to connect without needing a password:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> user@192.168.x.x</span></span></code></pre></div></li></ol></li></ol><h2 id="troubleshooting" tabindex="-1">Troubleshooting: <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to &quot;Troubleshooting:&quot;">​</a></h2><ul><li>Ensure both machines are on the same network.</li><li>If the connection fails, check the firewall settings on your Linux machine:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ufw</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> allow</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ssh</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ufw</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> enable</span></span></code></pre></div></li></ul><h2 id="details-on-public-key-copy-from-host-to-remote" tabindex="-1">Details on Public Key Copy from host to remote <a class="header-anchor" href="#details-on-public-key-copy-from-host-to-remote" aria-label="Permalink to &quot;Details on Public Key Copy from host to remote&quot;">​</a></h2><p>After generating the SSH key on your Mac, you need to copy the public key to your Linux machine. This allows you to log into the Linux machine without entering a password each time, enabling key-based authentication. Below are the detailed steps:</p><h2 id="_1-generate-the-ssh-key-on-your-mac-if-you-haven-t-done-it-already" tabindex="-1">1. <strong>Generate the SSH Key on Your Mac</strong> (if you haven’t done it already) <a class="header-anchor" href="#_1-generate-the-ssh-key-on-your-mac-if-you-haven-t-done-it-already" aria-label="Permalink to &quot;1. **Generate the SSH Key on Your Mac** (if you haven’t done it already)&quot;">​</a></h2><p>If you haven’t generated an SSH key, do it with the following command:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh-keygen</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rsa</span></span></code></pre></div><ul><li><strong>Explanation</strong>: <ul><li><code>-t rsa</code> specifies the type of key (RSA in this case).</li></ul></li><li>You will be prompted to enter a location to save the key. Press <strong>Enter</strong> to save it in the default location: <code>~/.ssh/id_rsa</code>.</li><li>You may also be asked to provide a passphrase for extra security. If you don&#39;t want a passphrase, just press <strong>Enter</strong> to leave it blank.</li></ul><h2 id="_2-view-the-generated-public-key" tabindex="-1">2. <strong>View the Generated Public Key</strong> <a class="header-anchor" href="#_2-view-the-generated-public-key" aria-label="Permalink to &quot;2. **View the Generated Public Key**&quot;">​</a></h2><p>You’ll now have two files in the <code>~/.ssh/</code> directory:</p><ul><li><code>id_rsa</code> (your private key)</li><li><code>id_rsa.pub</code> (your public key)</li></ul><p>To confirm the public key, you can view it with:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.ssh/id_rsa.pub</span></span></code></pre></div><h2 id="_3-copy-the-ssh-key-to-your-linux-machine" tabindex="-1">3. <strong>Copy the SSH Key to Your Linux Machine</strong> <a class="header-anchor" href="#_3-copy-the-ssh-key-to-your-linux-machine" aria-label="Permalink to &quot;3. **Copy the SSH Key to Your Linux Machine**&quot;">​</a></h2><p>The next step is to copy the public key to your Linux machine. This is where the <code>ssh-copy-id</code> command is used.</p><ol><li><p><strong>Make sure SSH is running</strong> on your Linux machine and you have its IP address and username ready.</p><ul><li>You can check the SSH service with:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> systemctl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> status</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ssh</span></span></code></pre></div></li></ul></li><li><p><strong>Copy the Public Key</strong> to the Linux machine:</p><p>In your Mac terminal, run the following command (replacing <code>user</code> with your Linux username and <code>192.168.x.x</code> with your Linux machine’s IP address):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh-copy-id</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> user@192.168.x.x</span></span></code></pre></div><ul><li><strong>Explanation</strong>: <ul><li><code>ssh-copy-id</code> is a convenient command that copies your public key to the Linux machine&#39;s <code>~/.ssh/authorized_keys</code> file, which is used for authentication.</li><li><code>user@192.168.x.x</code> is the Linux username and IP address you want to log in to.</li></ul></li></ul></li><li><p><strong>Authenticate with Your Password</strong>:</p><ul><li>You will be asked to enter the password for the Linux user account you are copying the SSH key to. This is necessary just for the first time to authorize the key copy.</li><li>Once the password is entered, <code>ssh-copy-id</code> will automatically add your public key to the Linux machine’s <code>~/.ssh/authorized_keys</code> file.</li></ul></li></ol><h2 id="_4-verify-ssh-key-based-authentication" tabindex="-1">4. <strong>Verify SSH Key-Based Authentication</strong> <a class="header-anchor" href="#_4-verify-ssh-key-based-authentication" aria-label="Permalink to &quot;4. **Verify SSH Key-Based Authentication**&quot;">​</a></h2><p>Now that the public key has been copied to the Linux machine, you can log in without needing a password.</p><ol><li><p>From your Mac, try SSHing into the Linux machine again:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> user@192.168.x.x</span></span></code></pre></div></li><li><p>This time, you should be logged in without being prompted for a password.</p></li></ol><h2 id="_5-optional-disable-password-authentication-on-the-linux-machine" tabindex="-1">5. <strong>(Optional) Disable Password Authentication</strong> on the Linux Machine <a class="header-anchor" href="#_5-optional-disable-password-authentication-on-the-linux-machine" aria-label="Permalink to &quot;5. **(Optional) Disable Password Authentication** on the Linux Machine&quot;">​</a></h2><p>For extra security, you may want to disable password-based SSH logins, ensuring only SSH keys can be used.</p><ol><li><p>On the Linux machine, open the SSH configuration file for editing:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nano</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /etc/ssh/sshd_config</span></span></code></pre></div></li><li><p>Find the following lines (uncomment them if needed) and change the values:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>PasswordAuthentication no</span></span>
<span class="line"><span>PermitRootLogin no</span></span></code></pre></div><p>This disables password logins and root login via SSH.</p></li><li><p><strong>Restart the SSH service</strong>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> systemctl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> restart</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ssh</span></span></code></pre></div></li></ol><p>Now, only key-based logins will be allowed, adding an extra layer of security.</p><h2 id="troubleshooting-1" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting-1" aria-label="Permalink to &quot;Troubleshooting&quot;">​</a></h2><ul><li><p><strong>Permission issues</strong>: Ensure the <code>~/.ssh/authorized_keys</code> file on the Linux machine has the correct permissions. Use:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">chmod</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 600</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.ssh/authorized_keys</span></span></code></pre></div></li><li><p><strong>Firewall</strong>: If you&#39;re unable to connect, ensure that the firewall on the Linux machine allows SSH:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ufw</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> allow</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ssh</span></span></code></pre></div></li></ul><p>That’s it! You’ve now set up passwordless SSH login using key-based authentication.</p><h2 id="using-networkmanager-to-manage-network-connections" tabindex="-1">Using <strong>NetworkManager</strong> to manage network connections <a class="header-anchor" href="#using-networkmanager-to-manage-network-connections" aria-label="Permalink to &quot;Using **NetworkManager** to manage network connections&quot;">​</a></h2><p>It is best to assign a static IP through NetworkManager directly rather than manually editing Netplan files.</p><p>Here&#39;s how you can assign a static IP using <strong>NetworkManager</strong>:</p><h2 id="method-1-using-nmtui-text-user-interface" tabindex="-1">Method 1: Using <code>nmtui</code> (Text User Interface) <a class="header-anchor" href="#method-1-using-nmtui-text-user-interface" aria-label="Permalink to &quot;Method 1: Using \`nmtui\` (Text User Interface)&quot;">​</a></h2><ol><li><p><strong>Open the Terminal</strong> on your Linux machine.</p></li><li><p><strong>Launch NetworkManager Text User Interface</strong>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nmtui</span></span></code></pre></div></li><li><p><strong>Select &quot;Edit a connection&quot;</strong>:</p><ul><li>Use the arrow keys to highlight this option and press <strong>Enter</strong>.</li></ul></li><li><p><strong>Choose Your Wi-Fi Network</strong>:</p><ul><li>You should see a list of available network connections (your <code>wlp3s0</code> Wi-Fi interface should be listed). Highlight your Wi-Fi connection and press <strong>Enter</strong>.</li></ul></li><li><p><strong>Set Static IP Address</strong>:</p><ul><li>In the <strong>Edit Connection</strong> window: <ul><li>Set the <strong>IPv4 Configuration</strong> method to <strong>Manual</strong>.</li><li>Enter the following details: <ul><li><strong>Addresses</strong>: The static IP you want to use (e.g., <code>192.168.1.100</code>).</li><li><strong>Netmask</strong>: Usually <code>255.255.255.0</code>.</li><li><strong>Gateway</strong>: The IP address of your router (e.g., <code>192.168.1.1</code>).</li><li><strong>DNS servers</strong>: For example, you can use Google&#39;s DNS (<code>8.8.8.8</code>).</li></ul></li></ul></li></ul></li><li><p><strong>Save the Configuration</strong>:</p><ul><li>Once you&#39;ve entered the IP details, move to <strong>OK</strong> using the arrow keys and press <strong>Enter</strong>.</li></ul></li><li><p><strong>Restart the Network</strong>: After saving the settings, you can restart the NetworkManager service to apply the changes:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> systemctl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> restart</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> NetworkManager</span></span></code></pre></div></li><li><p><strong>Verify Your IP Address</strong>: Check if the static IP has been assigned by running:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> a</span></span></code></pre></div></li></ol><h2 id="method-2-using-nmcli-command-line-interface" tabindex="-1">Method 2: Using <code>nmcli</code> (Command Line Interface) <a class="header-anchor" href="#method-2-using-nmcli-command-line-interface" aria-label="Permalink to &quot;Method 2: Using \`nmcli\` (Command Line Interface)&quot;">​</a></h2><p>If you prefer to work entirely in the command line, you can use the <code>nmcli</code> command to set a static IP address.</p><ol><li><p><strong>Check Your Wi-Fi Connection Name</strong>: Run the following command to list all available network connections:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nmcli</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> connection</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> show</span></span></code></pre></div><ul><li>Look for the <strong>NAME</strong> of your Wi-Fi connection (likely something like <code>wlp3s0</code>).</li></ul></li><li><p><strong>Set Static IP Address</strong>: Replace <code>wlp3s0</code> with your actual connection name. For example:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nmcli</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> con</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> modify</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> wlp3s0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ipv4.method</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> manual</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ipv4.addresses</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 192.168.1.100/24</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ipv4.gateway</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 192.168.1.1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ipv4.dns</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 8.8.8.8</span></span></code></pre></div><ul><li><code>192.168.1.100/24</code>: Your desired static IP address and subnet mask.</li><li><code>192.168.1.1</code>: Your router&#39;s IP address (gateway).</li><li><code>8.8.8.8</code>: DNS server.</li></ul></li><li><p><strong>Apply the Changes</strong>: To bring the connection down and up again to apply the new settings:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nmcli</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> con</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> down</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> wlp3s0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nmcli</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> con</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> wlp3s0</span></span></code></pre></div></li><li><p><strong>Verify Your IP Address</strong>: Run the following command to ensure the static IP has been assigned:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> a</span></span></code></pre></div></li></ol><p>If everything is configured correctly, your Linux machine should now use a static IP address over Wi-Fi. Let me know if you encounter any issues!</p><h2 id="easy-access-to-remote-without-typing-ip-everytime" tabindex="-1">Easy Access to Remote without typing IP everytime <a class="header-anchor" href="#easy-access-to-remote-without-typing-ip-everytime" aria-label="Permalink to &quot;Easy Access to Remote without typing IP everytime&quot;">​</a></h2><p>Updating the <strong><code>/etc/hosts</code></strong> file or the <strong>SSH config (<code>~/.ssh/config</code>)</strong> is useful if you frequently SSH into the same machine. These methods allow you to associate a <strong>hostname</strong> or a <strong>shortcut name</strong> with the IP address of your Linux machine, so you don’t need to remember or type the full IP address each time you SSH.</p><h2 id="_1-update-the-etc-hosts-file" tabindex="-1">1. <strong>Update the <code>/etc/hosts</code> File</strong> <a class="header-anchor" href="#_1-update-the-etc-hosts-file" aria-label="Permalink to &quot;1. **Update the \`/etc/hosts\` File**&quot;">​</a></h2><p>The <code>/etc/hosts</code> file allows you to map a hostname (alias) to an IP address. This means you can type the hostname instead of the IP address when you SSH or access the machine.</p><h3 id="steps" tabindex="-1">Steps: <a class="header-anchor" href="#steps" aria-label="Permalink to &quot;Steps:&quot;">​</a></h3><ol><li><p><strong>Open the Terminal</strong> on your Mac.</p></li><li><p><strong>Edit the <code>/etc/hosts</code> file</strong>:</p><ul><li>You will need superuser privileges to edit this file. Run the following command:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nano</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /etc/hosts</span></span></code></pre></div></li><li>You will be prompted to enter your password.</li></ul></li><li><p><strong>Add the IP Address and Hostname</strong>:</p><ul><li>At the bottom of the file, add a line with the new static IP of your Linux machine followed by a hostname (or alias) you want to use. For example:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">192.168.1.100</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> linuxpc</span></span></code></pre></div></li><li>Replace <code>192.168.1.100</code> with the new IP of your Linux machine and <code>linuxpc</code> with any alias you prefer.</li></ul></li><li><p><strong>Save the File</strong>:</p><ul><li>After adding the line, press <strong>Ctrl + O</strong> to save the file, then press <strong>Enter</strong>.</li><li>Press <strong>Ctrl + X</strong> to exit the editor.</li></ul></li><li><p><strong>Test the Change</strong>:</p><ul><li>Now, instead of using the IP address to SSH into your Linux machine, you can use the alias (hostname) you just set. For example:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> user@linuxpc</span></span></code></pre></div></li><li>Replace <code>user</code> with your Linux username and <code>linuxpc</code> with the alias you assigned.</li></ul><p>This way, even if you change the Linux machine&#39;s IP again, you can just update the <code>/etc/hosts</code> file with the new IP, and the alias (<code>linuxpc</code>) will still work.</p></li></ol><h2 id="_2-update-the-ssh-config-file-ssh-config" tabindex="-1">2. <strong>Update the SSH Config File (<code>~/.ssh/config</code>)</strong> <a class="header-anchor" href="#_2-update-the-ssh-config-file-ssh-config" aria-label="Permalink to &quot;2. **Update the SSH Config File (\`~/.ssh/config\`)**&quot;">​</a></h2><p>The SSH config file allows you to create shortcuts for SSH connections, where you can define specific settings for different hosts (like the IP address, username, port, etc.). This is very useful when you frequently SSH into multiple servers.</p><h3 id="steps-1" tabindex="-1">Steps: <a class="header-anchor" href="#steps-1" aria-label="Permalink to &quot;Steps:&quot;">​</a></h3><ol><li><p><strong>Open the Terminal</strong> on your Mac.</p></li><li><p><strong>Create/Edit the SSH Config File</strong>:</p><ul><li>The SSH config file is located in the <code>~/.ssh</code> directory. If it doesn&#39;t exist, you can create it. Run the following command to open it for editing:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nano</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.ssh/config</span></span></code></pre></div></li></ul></li><li><p><strong>Add SSH Configuration for Your Linux Machine</strong>:</p><ul><li>Add a new entry with the following format:<div class="language-plaintext vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">plaintext</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Host linuxpc</span></span>
<span class="line"><span>    HostName 192.168.1.100</span></span>
<span class="line"><span>    User your_username</span></span>
<span class="line"><span>    Port 22</span></span></code></pre></div><ul><li><strong>Host</strong>: This is the alias or shortcut you want to use for SSH. It can be any name you choose (e.g., <code>linuxpc</code>).</li><li><strong>HostName</strong>: The actual IP address or hostname of your Linux machine.</li><li><strong>User</strong>: Your username on the Linux machine (so you don’t need to type it every time).</li><li><strong>Port</strong>: The SSH port (default is <code>22</code>). If your SSH server is running on a different port, change this value.</li></ul></li></ul></li><li><p><strong>Save the File</strong>:</p><ul><li>After adding the configuration, press <strong>Ctrl + O</strong> to save the file, then press <strong>Enter</strong>.</li><li>Press <strong>Ctrl + X</strong> to exit the editor.</li></ul></li><li><p><strong>Set Correct Permissions for the Config File</strong>:</p><ul><li>SSH requires this file to have correct permissions. Run the following command to set the correct permissions:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">chmod</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 600</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.ssh/config</span></span></code></pre></div></li></ul></li><li><p><strong>Test the Change</strong>:</p><ul><li><p>Now, you can use the alias to SSH into your Linux machine without typing the full IP and username each time. For example:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> linuxpc</span></span></code></pre></div></li><li><p>This will automatically use the IP (<code>192.168.1.100</code>) and username you specified in the <code>~/.ssh/config</code> file.</p></li></ul></li></ol><h3 id="example-of-multiple-hosts-in-ssh-config" tabindex="-1">Example of Multiple Hosts in SSH Config: <a class="header-anchor" href="#example-of-multiple-hosts-in-ssh-config" aria-label="Permalink to &quot;Example of Multiple Hosts in SSH Config:&quot;">​</a></h3><p>If you connect to multiple servers, you can add multiple entries in your <code>~/.ssh/config</code> file, like so:</p><div class="language-plaintext vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">plaintext</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Host linuxpc</span></span>
<span class="line"><span>    HostName 192.168.1.100</span></span>
<span class="line"><span>    User your_username</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Host workserver</span></span>
<span class="line"><span>    HostName 203.0.113.10</span></span>
<span class="line"><span>    User your_work_username</span></span>
<span class="line"><span>    Port 2222</span></span></code></pre></div><ul><li>In this example, you can SSH into <code>linuxpc</code> or <code>workserver</code> with simple commands:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> linuxpc</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> workserver</span></span></code></pre></div></li></ul><h2 id="summary" tabindex="-1">Summary: <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary:&quot;">​</a></h2><ul><li><strong>Updating <code>/etc/hosts</code></strong> allows you to map a custom hostname (alias) to the IP address, so you can use the alias instead of the IP when connecting via SSH or accessing the machine on the network.</li><li><strong>Updating <code>~/.ssh/config</code></strong> allows you to create SSH shortcuts and store specific settings like username, IP address, and port, making it easier to SSH into frequently accessed machines.</li></ul><p>These methods save you time and prevent you from having to remember or type IP addresses repeatedly.</p><h2 id="save-passphrase-to-mac-keychain-for-easy-access" tabindex="-1">Save Passphrase to Mac KeyChain for easy access <a class="header-anchor" href="#save-passphrase-to-mac-keychain-for-easy-access" aria-label="Permalink to &quot;Save Passphrase to Mac KeyChain for easy access&quot;">​</a></h2><p>You can save the passphrase for your SSH key to avoid entering it every time you use the key for authentication. The most common way to achieve this is by using an <strong>SSH agent</strong>, which stores your passphrase in memory for the duration of your session.</p><p>Here’s how you can set this up on macOS:</p><h2 id="_1-start-the-ssh-agent" tabindex="-1">1. <strong>Start the SSH Agent</strong> <a class="header-anchor" href="#_1-start-the-ssh-agent" aria-label="Permalink to &quot;1. **Start the SSH Agent**&quot;">​</a></h2><p>The SSH agent is usually started by default on macOS, but you can ensure it’s running by starting it manually.</p><p>Open your terminal and run:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">eval</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh-agent</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">)&quot;</span></span></code></pre></div><p>This will start the SSH agent and return the process ID (PID), indicating that it&#39;s running.</p><h2 id="_2-add-your-ssh-private-key-to-the-agent" tabindex="-1">2. <strong>Add Your SSH Private Key to the Agent</strong> <a class="header-anchor" href="#_2-add-your-ssh-private-key-to-the-agent" aria-label="Permalink to &quot;2. **Add Your SSH Private Key to the Agent**&quot;">​</a></h2><p>Once the SSH agent is running, you need to add your private key to it. This allows the agent to cache the key and manage the passphrase for you.</p><p>To add your private key:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh-add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.ssh/id_rsa</span></span></code></pre></div><ul><li>Replace <code>~/.ssh/id_rsa</code> with the path to your private key if it’s located somewhere else.</li></ul><p>You will be prompted to enter your passphrase one last time. After that, the agent will store the passphrase for the duration of your session, so you won’t have to enter it again.</p><h2 id="_3-configure-ssh-to-automatically-use-the-agent" tabindex="-1">3. <strong>Configure SSH to Automatically Use the Agent</strong> <a class="header-anchor" href="#_3-configure-ssh-to-automatically-use-the-agent" aria-label="Permalink to &quot;3. **Configure SSH to Automatically Use the Agent**&quot;">​</a></h2><p>To make the SSH agent automatically load your key when the system starts, you can modify or create a configuration file:</p><ol><li><p><strong>Open or create the SSH config file</strong>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nano</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.ssh/config</span></span></code></pre></div></li><li><p><strong>Add the following lines</strong>:</p><div class="language-plaintext vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">plaintext</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Host *</span></span>
<span class="line"><span>    AddKeysToAgent yes</span></span>
<span class="line"><span>    UseKeychain yes</span></span>
<span class="line"><span>    IdentityFile ~/.ssh/id_rsa</span></span></code></pre></div><ul><li><code>AddKeysToAgent yes</code>: This tells SSH to automatically add keys to the agent.</li><li><code>UseKeychain yes</code>: On macOS, this stores the passphrase in the macOS Keychain.</li><li><code>IdentityFile ~/.ssh/id_rsa</code>: Specifies the location of your private key. Change the path if your key is located elsewhere.</li></ul></li><li><p><strong>Save and close the file</strong> by pressing <strong>Ctrl + O</strong> to save and <strong>Ctrl + X</strong> to exit.</p></li></ol><h2 id="_4-store-the-passphrase-in-macos-keychain" tabindex="-1">4. <strong>Store the Passphrase in macOS Keychain</strong> <a class="header-anchor" href="#_4-store-the-passphrase-in-macos-keychain" aria-label="Permalink to &quot;4. **Store the Passphrase in macOS Keychain**&quot;">​</a></h2><p>macOS allows you to store your SSH key passphrase in the Keychain, so you don’t need to enter it again after rebooting. You can do this by using the <code>ssh-add</code> command with the <code>-K</code> option (on older macOS versions) or simply by following the steps above (<code>UseKeychain yes</code>).</p><p>To store the passphrase in the macOS Keychain:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh-add</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --apple-use-keychain</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.ssh/id_rsa</span></span></code></pre></div><ul><li>After running this command, the passphrase will be saved to the Keychain, so you won’t need to enter it again for future logins.</li></ul><p>On <strong>macOS Monterey (12.x)</strong> and later, <code>ssh-add</code> automatically uses the Keychain without needing the <code>-K</code> option.</p><h2 id="_5-verify-ssh-key-is-loaded" tabindex="-1">5. <strong>Verify SSH Key is Loaded</strong> <a class="header-anchor" href="#_5-verify-ssh-key-is-loaded" aria-label="Permalink to &quot;5. **Verify SSH Key is Loaded**&quot;">​</a></h2><p>To check that your key has been successfully added to the agent, run:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh-add</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -l</span></span></code></pre></div><p>This will list the keys currently loaded in the SSH agent. You should see your key listed here.</p><h2 id="_6-restarting-the-ssh-agent-automatically" tabindex="-1">6. <strong>Restarting the SSH Agent Automatically</strong> <a class="header-anchor" href="#_6-restarting-the-ssh-agent-automatically" aria-label="Permalink to &quot;6. **Restarting the SSH Agent Automatically**&quot;">​</a></h2><p>To ensure your keys are automatically loaded into the SSH agent on system startup, you can add the following to your shell’s configuration file (<code>.bash_profile</code>, <code>.zshrc</code>, etc.):</p><ol><li><p><strong>Open your shell’s config file</strong>: For example, if you are using <code>zsh</code>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nano</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.zshrc</span></span></code></pre></div></li><li><p><strong>Add the following lines</strong>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">eval</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh-agent</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">)&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh-add</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -A</span></span></code></pre></div><ul><li><code>ssh-add -A</code>: Automatically adds all identities stored in the macOS Keychain to the SSH agent when a new session is started.</li></ul></li><li><p><strong>Save and close the file</strong>.</p></li></ol><p>Now, every time you start a new terminal session or log into your Mac, the SSH agent will automatically start, and your keys will be loaded from the Keychain.</p><hr><p>By following these steps, you’ll avoid having to enter the passphrase for your SSH key every time you SSH into a machine, without compromising the security of your key.</p>`,90)]))}const g=e(n,[["render",o]]);export{u as __pageData,g as default};
